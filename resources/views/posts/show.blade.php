@extends('partials.app')@section('seo_title',$post->seo_title ? $post->seo_title : $post->title)@section('meta_keywords',$post->meta_keywords)@section('meta_description',$post->meta_description)@section('content') <div class="sales-page def-page"> <div class="pre-header"> <div class="container"> @include('partials.breadcrumbs',['title'=>'Новости','subtitle'=> $post->title,'titleLink'=> route('posts.index')]) <h1>{{$post->title}}</h1> </div></div><div class="container"> <div class="row"> <div class="content col-12"> <div class="about-content"> <div class="text-content"> <div class="row page-header"> <div class="col-5"> <picture> <source srcset="{{str_replace(pathinfo(Voyager::image($post->image), PATHINFO_EXTENSION),'webp',Voyager::image($post->image))}}" type="image/webp"> <source srcset="{{Voyager::image($post->image)}}" type="image/jpeg"> <img src="{{Voyager::image($post->image)}}" alt="{{$post->title}}"> </picture> </div><div class="col-7"> <h2>{{$post->title}}</h2> <p>{{$post->excerpt}}</p></div></div>{!! $post->body !!}</div></div></div></div></div></div>@endsection